// This file is meant to serve as copy-paste fodder for creating new components.

/**
 * Creates an empty component
 * @returns {Template}
 * @constructor
 */
function Template (){

    var ownContext = new HTMLCanvasElement();

    this.update = function(context, timediff, timestamp) {

    }
    this.draw = function (context, timediff, timestamp){

    }

    return this;
}